import "../../io"

fun putstr(s str) / sys {
	with io.unix(), io.stdout() {
		io.write(cast s);
	}
}

fun putint(n int) / sys {
	with io.unix(), io.stdout() {
		io.write_int(n)
		io.write("\n");
	}
}

fun wait() / sys {
	with io.unix(), io.stdin() {
		io.read([---]);
	}
}
