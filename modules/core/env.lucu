--# Command line arguments iterator
effect Args = {
    fun peek_arg ?cstr[static,]
    fun skip_arg
    fun remaining_args with Yield[cstr[static,],]
}

fun has_next_arg bool with Args = peek_arg != none

fun next_arg ?cstr[static,] with Args = {
    let next = peek_arg
    skip_arg
    next
}

--# Environment variables map
effect Environ = {
    fun get[r](key str[r,]) ?cstr[static,]
}

fun contains[r](key str[r,]) bool with Environ = get(key) != none
