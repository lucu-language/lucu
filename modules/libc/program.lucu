import "builtin:regions"
import "builtin:types"

effect Termination = {
	fun abort !
	fun exit(exit_code int) !
	fun quick_exit(exit_code int) !
	fun _Exit(exit_code int) !

	-- const EXIT_SUCCESS int
	-- const EXIT_FAILURE int

	-- effect World
	-- fun atexit(func ^fun with World)
	-- fun at_quick_exit(func ^fun with World)
}

fun unreachable ! = {
}

-- effect Environment = {
-- 	region ENV
-- 	handle regions.Read[ENV]
-- 	fun system(command ?string) int
-- 	fun getenv(name string) ?string[ENV]
-- 	fun getenv_s(len ^#write size_t, value ^#write []char, name string) errno_t
-- }

-- fun memalignment(p ?^#noread types.unit) size_t = {
-- }

-- effect Signal {
-- 	effect World
-- 	const SIG_DFL ^fun(sig int) with World
-- 	const SIG_IGN ^fun(sig int) with World
-- 	const SIG_ERR ^fun(sig int) with World
-- 	const SIGTERM int
-- 	const SIGSEGV int
-- 	const SIGINT int
-- 	const SIGILL int
-- 	const SIGABRT int
-- 	const SIGFPE int
-- 	fun signal(sig int, handler ^fun(sig int) with World)
-- 	fun raise(sig int) int
-- 	type sig_atomic_t
-- }

-- effect Setjmp {
-- 	type jmp_buf
-- 	fun setjmp(fun body(env jmp_buf, status int))
-- 	fun longjmp(env jmp_buf, status int) !
-- }
