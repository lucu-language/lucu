effect counter {
	fun count() int
}

fun init_counter(start int) handle counter {
	mut n = start
	fun { n++ }
}

fun main() / debug {
	with init_counter(5) {
		putint(count()) # 5
		putint(count()) # 6
		putint(count()) # 7
	}
}
