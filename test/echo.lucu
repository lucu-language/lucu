import "core:io"
import "builtin:regions"

fun main with io.StdIo = {
    use io.stdin
    use io.stdout

    let buffer ^[]i8 = use regions.alloca(1000)
    let total = io.read_line(buffer)
    discard io.write(buffer[..total])
}
