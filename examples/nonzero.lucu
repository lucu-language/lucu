import "core:io"

effect nonzero {
	fun nz(n int) int
}

handle nonzero fails {
	fun nz(n int) int {
		if n == 0 {
			fail
		} else {
			n
		}
	}
}

fun main() / io.stdio {
	io.putint(nz(7))
	io.putint(nz(2))
	io.putint(nz(0))
	io.putint(nz(8))
}
